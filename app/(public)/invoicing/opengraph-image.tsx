/**
 * /invoicing OG image — explicit route-level file-convention image so that
 * og:image is emitted even though the page exports its own openGraph object.
 */
export {
	default,
	alt,
	size,
	contentType,
} from '../opengraph-image';
