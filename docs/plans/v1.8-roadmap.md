1. The amount fields does not allow empty during input resulting to a difficult user experience because when its cleared out to add new input it would automatically input the old amount - Done
2. To have a separation for the payment for liabilities account instead of just a centralized transfer - Done
3. Set up the SMTP using google since its free - In Progress (Switched to Resend, design at docs/plans/2026-02-07-resend-email-integration-design.md)
4. We need to enhance the monthly reporting and if possible to include an auto cron job
5. Replicate Budget from previous month to new month - Done
6. Have the multiple category of expenses or create a new report or enhance any existing report for income and expenses pattern - Done (Income vs Expense Ratio Chart in P&L tab)
7. Will swap the Funds card and Recent transactions and accounts (Or let agent frontend developer re-design the dashboard based on UI/UX best practices) - Done (Funds moved above Recent Transactions)
8. Expenses/Income standalone export (Filterable by category) - Done
9. Automatic reports or task that will be scheduled to run
10. Forgot password and lets have a google auth - In Progress (design at docs/plans/2026-02-07-forgot-password-google-auth-design.md, depends on #3)
11. Redesign the changelog architecturally on how we store the changes logs because it's getting long
12. Looks like we need a clear separation for liabilities, liquid and funds type of accounts - Done (4-group classification: Liquid, Savings, Liabilities, Funds)
13. Filter on expenses based on category - Done
14. A separate report or modify existing where the main goal of this is to create a health check on the user's financial (Ask the accountant agent) - Done (Financial Health Check tab with 5-pillar scoring + roast feedback)
15. How does the Emergency Fund being calculated and display on the dashboard card? - Done
16. During transfer when I choose a liability account it shows me my debt balance instead of my current credit limit - Done (liability separation + ledger fix)
17. Form where user can request a new feature
18. Create a claude code workflow where every commit or push it will run a workflow to update the changelog related files and page
19. Make sure to redesign the sidebar reducing the clotting of many menus - Done (Transactions group)
20. When clicking a specific budget to display the transaction list. Instead of only seeing the transaction on that budget as well as the status cards we can see the whole transaction of all budgets as well as the status card - Done (budget scoped to budgetId + unlinked expenses section)
21. When creating a new expense I noticed that on the budget dropdown we still include the budget from the previous month and may cause confusion let the accountant agent draft for high level thats financial related and then to be implemented by the engineer agent - Done (drill-down BudgetSelector with current month default + "Choose another month" navigation)
22. Thinking of merging the Health Check into the overview and may remove not needed components on the overview. Ask accountant agent if does it make sense on financial perspective - Done (Health Check merged into Overview tab, Fund Health removed from reports, inline health badge, collapsible Net Worth chart + pillar breakdown, auto-expand weakest pillar with nudge)
